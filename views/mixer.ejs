<!DOCTYPE html>
<html>
<head>
  <title>Logic Mixer State</title>
  <style>
    body { font-family: Arial; background: #111; color: #eee; padding: 2rem; }
    table { width: 100%; border-collapse: collapse; }
    th, td { padding: 8px 16px; border-bottom: 1px solid #333; text-align: left; }
    th { background-color: #222; }
  </style>
</head>
<body>
  <h1>🎚️ Logic Mixer Snapshot</h1>
  <table>
    <thead>
      <tr>
        <th>Track</th>
        <th>Name</th>
        <th>Volume</th>
        <th>Pan</th>
        <th>Mute</th>
        <th>Solo</th>
        <th>Record</th>
      </tr>
    </thead>
    <tbody>
      <% for (let ch in mixer) { %>
        <tr>
          <td><%= ch %></td>
          <td><%= mixer[ch].name || '—' %></td>
          <td><%= mixer[ch].volume ?? '—' %></td>
          <td><%= mixer[ch].pan ?? '—' %></td>
          <td><%= mixer[ch].mute ? '✅' : '❌' %></td>
          <td><%= mixer[ch].solo ? '✅' : '❌' %></td>
          <td><%= mixer[ch].recordArm ? '🎙️' : '—' %></td>
        </tr>
      <% } %>
    </tbody>
  </table>
</body>
</html>
